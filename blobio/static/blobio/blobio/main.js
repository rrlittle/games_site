var viewWidth = 600;
var viewHeight = 400;

var x;
var y;

var blobs;

function setup(){
	createCanvas(viewWidth,viewHeight);
}

function draw(){
	background(0);
	// for (var i = 0; i < blobs.length; i++) {
		// var b = blobs[i]
	// }
	// ellipse(x,y,50,50);
}

function update(){
	// mouse = createVector(mouseX, mouseY);
	// vel = pos.sub(mouse); 
}






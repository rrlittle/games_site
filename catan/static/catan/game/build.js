$(document).ready(function(){
	// when a building button is clicked
	$('.building-btn').click(function(){
		// check if you have the resources neccessary
		// close the modal
		$('#buildModal').modal('hide');
		// update currentpiece in main.js to start
		// drawing the building under the mouse
		// wait for when the user selects the vertex to place it
	});
});